export type Expense = {
    id: string;
    expense: string;
    description: string;
    amount: string;
    date: string;
    category_id: string;
    type_id: string;
    card_id: string;
    installments: Number;
}